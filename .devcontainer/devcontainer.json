{
  "name": "rmshelper",
  "dockerFile": "Dockerfile",
  "appPort": "9000:9000",
  "context": "..",
  "extensions": [
    "ms-python.python"
  ],
  "settings": {
    "python.pythonPath": "/usr/local/bin/python"
  },
  "runArgs": [
    "-v",
    "${env:HOME}${env:USERPROFILE}/.aws:/root/.aws"
  ]
}